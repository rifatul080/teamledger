{
  "name": "teamledger-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies internet requests to the TeamLedger FastAPI container.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "types": "wrangler types",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.137.0"
  }
}
