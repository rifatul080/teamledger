# Empty marker file: makes `app` a regular (not implicit-namespace) Python
# package. The Dockerfile copies this file explicitly as part of the
# production image, so it must exist on disk and be tracked by git.
